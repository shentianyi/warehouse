<%= form_for(@location_container_order) do |f| %>
  <% if @location_container_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_container_order.errors.count, "error") %> prohibited this location_container_order from being saved:</h2>

      <ul>
      <% @location_container_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location_container_id %><br>
    <%= f.text_field :location_container_id %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.text_field :order_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
