<%= form_for(@user_permission_group) do |f| %>
  <% if @user_permission_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_permission_group.errors.count, "error") %> prohibited this user_permission_group from being saved:</h2>

      <ul>
      <% @user_permission_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :permission_group_id %><br>
    <%= f.text_field :permission_group_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
