<style>
    .col-sm-12 {
        height: 40px;
    }

    select {
        width: 150px;
    }

    a:hover, a:active {
        text-decoration: none;
        background-color: white;
        color: black;
        outline: none;
    }
</style>
<%= form_for(@inventory_list) do |f| %>
    <% if @inventory_list.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@inventory_list.errors.count, "error") %> prohibited this inventory_list from being
            saved:</h2>

          <ul>
            <% @inventory_list.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="panel panel-primary">
      <div class="panel-body">
        <div class="col-sm-12" style="border-bottom: 1px dashed #000;">
          <div class="col-sm-4">
            <%= f.label :name, "名称" %>
            <%= f.text_field :name %>
          </div>
          <div class="col-sm-4">
            <%= f.label :state ,"状态"%>
            <%= f.select(:state, InventoryListState.menu, {include_blank: false}) %>
          </div>
          <div class="col-sm-4">
            <%= f.label :whouse_id,"仓库" %>
            <%= f.select(:whouse_id, Whouse.all.collect { |w| [w.nr, w.nr] }, {include_blank: false}) %>
          </div>
        </div>

        <div class="col-sm-12">
          <div class="actions">
            <input type="submit" class="btn btn-primary" value="完成" name="commit" style="margin-top: 10px;width: 150px;color: white;">
          </div>
        </div>
      </div>
    </div>
<% end %>
