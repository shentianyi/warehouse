<%= form_for(@package_type) do |f| %>
    <% if @package_type.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@package_type.errors.count, "error") %> prohibited this package_type from being saved:</h2>

          <ul>
            <% @package_type.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>


    <div class="panel panel-primary">
      <div class="panel-body">
        <div class="col-sm-12" style="height: 40px;border-bottom: 1px dashed #000;">
          <div class="col-sm-4">
            <%= f.label :nr, '编 号' %>
            <%= f.text_field :nr %>
          </div>
          <div class="col-sm-4">
            <%= f.label :name, '名 称' %>
            <%= f.text_field :name %>
          </div>
        </div>
        <div class="actions">
          <%= f.submit value: '提 交', class: 'btn btn-primary', :style => 'width:200px;margin-top:20px;' %>
        </div>
      </div>
    </div>
<% end %>
