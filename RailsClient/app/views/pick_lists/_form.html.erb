<%= form_for(@pick_list) do |f| %>
  <% if @pick_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pick_list.errors.count, "error") %> prohibited this pick_list from being saved:</h2>

      <ul>
      <% @pick_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br>
    <%= f.text_field :id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
