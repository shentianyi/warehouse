<%= form_for(@part_type) do |f| %>
  <% if @part_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@part_type.errors.count, "error") %> prohibited this part_type from being saved:</h2>

      <ul>
      <% @part_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :id,'编 号' %><br>
      <%= f.text_field :nr %>
    </div>
  <div class="field">
    <%= f.label :name,'名 称' %><br>
    <%= f.text_field :name %>
  </div>
    <div class="actions">
      <%= f.submit value: '提 交', class: 'btn btn-primary btn-lg' %>
    </div>
<% end %>
