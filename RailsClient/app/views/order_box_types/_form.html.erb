<%= form_for(@order_box_type) do |f| %>
    <% if @order_box_type.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@order_box_type.errors.count, "error") %> prohibited this order_box_type from being
            saved:</h2>

          <ul>
            <% @order_box_type.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :description %><br>
      <%= f.text_field :description %>
    </div>
    <div class="field">
      <%= f.label :weight %><br>
      <%= f.text_field :weight %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
