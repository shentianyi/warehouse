<h1><%= @package.id%></h1>
<span class="label label-primary"><%= @package.id%></span>

<h4>状态: <small><%= PackageState.display(@package.state) %></small></h4>
<h4>零件号: <small><%= PackageLabelRegex.part_prefix_string + @package.part_id %></small></h4>
<h4>数量: <small><%= PackageLabelRegex.quantity_prefix_string+@package.quantity_str %></small></h4>
<h4>入库时间: <small><%= @package.check_in_time %></small></h4>
<h4>发货地: <small><%= @package.location.name %></small></h4>
<h4>备货员工: <small><%=  @package.user.name if @package.user_id %></small></h4>
<h4>备货员工号: <small><%= @package.user_id if @package.user_id %></small></h4>
<h4>所属托清单: <small><%=  @package.forklift_id if @package.forklift_id %></small></h4>
<h4>上架库位: <small><%= @package.position.whouse.name + ' '+@package.position.detail if @package.position %></small></h4>

<%= link_to '编辑', edit_package_path(@package) ,:class=>'btn btn-default'%> |
<%= link_to '返回', packages_path,:class=>'btn btn-default' %>
