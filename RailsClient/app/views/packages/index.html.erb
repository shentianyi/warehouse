<h2>包装箱列表</h2>

<table class="table table-bordered table-striped globle-table">
  <thead>
    <tr>
      <th>序号</th>
      <th>编号</th>
      <th>零件号</th>
      <th>数量</th>
      <th>状态</th>
      <th>所属位置</th>
      <th>创建者</th>
      <th>所属清单</th>
      <th>输入时间</th>
      <th>上架位置</th>
    </tr>
  </thead>

  <tbody>
    <% @packages.each_with_index do |package,index| %>
      <tr>
        <td colspan="3"><%= index %>
        <td colspan="3"><%= package.id %></td>
        <td colspan="3"><%= package.part_id%></td>
        <td colspan="3"><%= package.quantity_str %></td>
        <td colspan="3"><%= PackageState.display(package.state) %></td>
        <td colspan="3"><%= package.location_id %></td>
        <td colspan="3"><%= package.user_id %></td>
        <td colspan="3"><%= link_to package.forklift_id, package.forklift  if package.forklift%></td>
        <td colspan="3"><%= package.check_in_time %></td>
        <td colspan="3"><%= package.position.whouse.name + ' ' +package.position.detail if package.position %></td>
        <td><%= link_to '编辑', edit_package_path(package) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @packages, :page_links => false %>