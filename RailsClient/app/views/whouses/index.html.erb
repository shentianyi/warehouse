<h2>仓库列表</h2>

<%= link_to "新建分仓库", send("new_#{@model}_path") %>
<%= link_to "上传/更新分仓库", send("import_#{@model.pluralize}_path") %>
<%= link_to "下载", send("download_#{@model.pluralize}_path") %>
<div class="digg_pagination">
  <div class="page_info">
    <%= page_entries_info @whouses %>
  </div>
  <%= will_paginate @whouses, :container => false %>
</div>
<table class="table table-bordered table-striped globle-table">
  <thead>
    <tr>
      <th>序号</th>
      <th>编号</th>
      <th>名称</th>
      <th>所属地点</th>
      <th colspan="3">&nbsp;</th>
    </tr>
  </thead>

  <tbody>
    <% @whouses.each_with_index do |whouse,index| %>
      <tr>
        <td><%= index +@whouses.offset+1%>
        <td><%= whouse.id %> </td>
        <td><%= whouse.name %></td>
        <td><%= whouse.location.name if whouse.location %></td>
        <td><%= link_to '详细', whouse %></td>
        <td><%= link_to '编辑', edit_whouse_path(whouse) %></td>
        <td><%= link_to '删除', whouse, method: :delete, data: { confirm: '确认删除?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<div class="digg_pagination">
  <%= will_paginate @whouses, :container => false %>
</div>
<br>
