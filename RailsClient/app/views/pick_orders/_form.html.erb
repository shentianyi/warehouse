<%= form_for(@pick_order) do |f| %>
  <% if @pick_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pick_order.errors.count, "error") %> prohibited this pick_order from being saved:</h2>

      <ul>
      <% @pick_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.text_field :order_id %>
  </div>
  <div class="field">
    <%= f.label :pick_list_id %><br>
    <%= f.text_field :pick_list_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
